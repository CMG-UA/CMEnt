% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmrs_interaction.R
\name{extractDMRMotifs}
\alias{extractDMRMotifs}
\title{Extract DMR Motif Frequencies}
\usage{
extractDMRMotifs(
  dmrs,
  genome,
  array = "450k",
  beta_locs = NULL,
  flank_size = 5,
  plot.dir = NULL
)
}
\arguments{
\item{dmrs}{Dataframe or GRanges object containing DMR coordinates and CpG indices}

\item{genome}{Character. Genome version to use for sequence extraction (e.g., "hg19")}

\item{array}{Character. Array platform type (e.g., "450K", "EPIC"). Must be NULL if input is not array-based. (default: "450K")}

\item{beta_locs}{Data frame. Optional pre-computed genomic locations. If NULL,
locations will be retrieved using getSortedGenomicLocs (default: NULL)}

\item{flank_size}{Integer. Number of base pairs to include as flanking regions around each CpG site (default: 5)}
}
\value{
The input Dataframe/GRanges object with an additional metadata column:
\itemize{
\item pwm: A matrix of base frequencies (rows: positions relative to CpG, columns: bases A, C, G, T)
\item consensus_seq: A character string representing the consensus sequence derived from the PWM
}
}
\description{
Extracts motif frequencies around CpG sites within DMRs.
For each DMR, retrieves sequences around the start and end CpG sites,
calculates base frequencies at each position, and stores the results in
the DMR metadata.
}
\examples{
# Extract motif frequencies for DMRs
dmrs <- data.frame(
    chr = c("chr16", "chr3"),
    start = c(53468112, 37459206),
    end = c(53468712, 37493431),
    start_cpg = c("cg00000029", "cg00000108"),
    start_seed = c("cg00000029", "cg00000108"),
    end_cpg = c("cg13426503", "cg08730726"),
    end_seed = c("cg13426503", "cg08730726"),
    seeds = c("cg00000029,cg13426503", "cg00000108,cg08730726")
)
dmrs_with_motifs <- extractDMRMotifs(dmrs, genome = "hg19", array = "450K")
# Access motif frequencies for the first DMR
motif_freqs_dmr1 <- dmrs_with_motifs$pwm[[1]]
}
