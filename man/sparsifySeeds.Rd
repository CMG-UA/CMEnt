% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparsify_seeds.R
\name{sparsifySeeds}
\alias{sparsifySeeds}
\title{Sparsify seed locations by minimum distance}
\usage{
sparsifySeeds(seeds, min_distance)
}
\arguments{
\item{seeds}{Data frame with genomic seed locations containing \code{chr} and \code{start} columns.}

\item{min_distance}{Non-negative numeric scalar. Seeds with a distance below this
value to both neighboring seeds on the same chromosome are removed.}
}
\value{
A subset of \code{seeds} with original columns and row names preserved.
}
\description{
Removes seeds inside dense runs where neighboring seeds are closer than a
minimum distance on the same chromosome.
}
\details{
Seeds are compared per chromosome after sorting by \code{start}. A seed is removed
only when both the previous and next seed on that chromosome are closer than
\code{min_distance}. Endpoints of close runs are retained. Seeds at exactly
\code{min_distance} are retained.
}
\examples{
seeds <- data.frame(
    chr = c("chr1", "chr1", "chr1"),
    start = c(100, 105, 120)
)
sparsifySeeds(seeds, min_distance = 10)

}
