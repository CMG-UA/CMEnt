% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dmrs.R
\name{plotDMRsManhattan}
\alias{plotDMRsManhattan}
\title{Plot Manhattan-Style View of DMR Scores}
\usage{
plotDMRsManhattan(
  dmrs,
  genome = "hg38",
  promoter_col = "in_promoter_of",
  gene_body_col = "in_gene_body_of",
  point_size = 1.1,
  point_alpha = 0.75,
  block_col = "block_id",
  show_blocks = TRUE,
  block_alpha = 0.12,
  block_linewidth = 0.25,
  output_file = NULL,
  width = 12,
  height = 6
)
}
\arguments{
\item{dmrs}{GRanges object or data frame. DMR results from findDMRsFromSeeds.}
\item{genome}{Character. Genome version passed to \code{convertToGRanges} (default: \code{"hg38"}).}

\item{promoter_col}{Character. Metadata column indicating promoter overlap (default: \code{"in_promoter_of"}).}

\item{gene_body_col}{Character. Metadata column indicating gene-body overlap (default: \code{"in_gene_body_of"}).}

\item{point_size}{Numeric. Point size (default: \code{1.1}).}

\item{point_alpha}{Numeric. Point alpha in \verb{[0, 1]} (default: \code{0.75}).}

\item{block_col}{Character. Metadata column containing block IDs (default: \code{"block_id"}).}

\item{show_blocks}{Logical. If TRUE, draw translucent rectangles for identified blocks (default: \code{TRUE}).}

\item{block_alpha}{Numeric. Alpha for block rectangles in \verb{[0, 1]} (default: \code{0.12}).}

\item{block_linewidth}{Numeric. Line width for block rectangle borders (default: \code{0.25}).}

\item{output_file}{Character or NULL. If non-NULL, path to save the plot as a PDF (default: \code{NULL}).}

\item{width}{Numeric. Width of the output PDF in inches (default: \code{12}).}

\item{height}{Numeric. Height of the output PDF in inches (default: \code{6}).}
}
\value{
A ggplot object.
}
\description{
Creates a Manhattan-style genome-wide scatter plot of DMR scores.
DMRs are colored by their dominant genomic region class inferred from DMR annotations.
}
\examples{
\dontrun{
dmrs <- readRDS("dmrs.rds")
p <- plotDMRsManhattan(dmrs)
print(p)
}
}
