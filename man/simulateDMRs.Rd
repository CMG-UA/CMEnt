% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_dmrs.R
\name{simulateDMRs}
\alias{simulateDMRs}
\title{Simulate DMRs while preserving local methylation structure}
\usage{
simulateDMRs(
  beta,
  num_dmrs = 3000L,
  delta_max0 = 0.4,
  groups = NULL,
  case_group = NULL,
  samplesheet = NULL,
  samplesheet_sep = "\\t",
  sample_group_col = "Sample_Group",
  covariates = NULL,
  max_gap = 500L,
  min_sites = 5L,
  max_sites = 500L,
  truth_min_delta_beta = 0,
  delta_jitter = 1/3,
  expected_correlation = 0.7,
  neighbor_window = 5L,
  profile_degree = 4L,
  flank_fraction = 0.2,
  resample_counts = TRUE,
  array = c("450K", "27K", "EPIC", "EPICv2"),
  genome = c("hg38", "hg19", "hs1", "mm10", "mm39"),
  sorted_locs = NULL,
  beta_row_names_file = NULL,
  chrom_col = "#chrom",
  start_col = "start",
  njobs = getOption("CMEnt.njobs", min(8, future::availableCores() - 1)),
  verbose = getOption("CMEnt.verbose", 1)
)
}
\arguments{
\item{beta}{A BSseq object, a \link{BetaHandler} object, a beta matrix/data
frame, a path to a beta/tabix file, or a path to an \code{.rds} file
containing a BSseq object.}

\item{num_dmrs}{Number of DMRs to spike in.}

\item{delta_max0}{Baseline maximum beta-scale effect size near the center of
each DMR.}

\item{groups}{Optional sample group vector. If \code{NULL}, the first half of
samples are assigned to \code{Condition1} and the remaining samples to
\code{Condition2}.}

\item{case_group}{Group receiving the differential shift. Defaults to the
second group level.}

\item{samplesheet}{Optional data frame or path to a tab-delimited sample
sheet with covariates. Row names, a \code{Sample_ID} column, or an unnamed first
column must identify samples.}

\item{samplesheet_sep}{Separator for samplesheet files. Default is tab.}

\item{sample_group_col}{Name of the phenotype column added to the returned
samplesheet. Values are \code{"case"} for perturbed samples and \code{"control"} for
all other samples.}

\item{covariates}{Optional covariate columns in \code{samplesheet} to regress out
on the logit methylation scale before DMRs are simulated. The per-site
baseline is retained.}

\item{max_gap}{Maximum gap, in bp, used to form candidate site segments.}

\item{min_sites}{Minimum number of sites per candidate DMR segment.}

\item{max_sites}{Maximum number of sites per candidate DMR segment.}

\item{truth_min_delta_beta}{Minimum intended beta-scale perturbation for a
site to define the reported truth interval. The default, \code{0}, reports the
full selected segment so simulated flanks are not treated as false
positives during benchmarking.}

\item{delta_jitter}{Width of the random effect-size jitter around
\code{delta_max0}.}

\item{expected_correlation}{Minimum within-DMR expected correlation target.
The per-DMR target is the larger of this value and the estimated local
background correlation, capped to the interval \verb{[0, 0.99]}.}

\item{neighbor_window}{Number of neighboring sites used to smooth the
index-based correlated random effect inside each DMR. A value of \code{5}
uses up to two upstream and two downstream neighboring sites.}

\item{profile_degree}{Degree used by the triweight profile.}

\item{flank_fraction}{Fraction of the selected segment width added on both
sides before evaluating the triweight profile.}

\item{resample_counts}{If \code{TRUE}, methylated counts in touched regions are
redrawn from shifted probabilities and coverage. If \code{FALSE}, counts are
rounded deterministically.}

\item{array}{Array platform type. Used only when \code{beta} is not a BSseq object
or a self-contained \link{BetaHandler}.}

\item{genome}{Reference genome. Used only when \code{beta} is not a BSseq object
or a self-contained \link{BetaHandler}.}

\item{sorted_locs}{Optional genomic locations with \code{chr}, \code{start}, and
optionally \code{end} columns. Used only for non-BSseq inputs.}

\item{beta_row_names_file}{Optional file with beta row names. Used only for
non-BSseq file inputs.}

\item{chrom_col}{Chromosome column name for tabix inputs.}

\item{start_col}{Start column name for tabix inputs.}

\item{njobs}{Number of parallel jobs for loading non-BSseq inputs.}

\item{verbose}{Numeric. Logging verbosity level. Default comes from
\code{getOption("CMEnt.verbose")}.}
}
\value{
A list with simulated output (\code{simulated}), optional genomic
locations for non-BSseq inputs (\code{beta_locs}), phenotype data (\code{pheno}),
and dmrseq-like metadata:
\code{gr.dmrs}, \code{dmr.mncov}, \code{dmr.L}, \code{delta}, \code{truth}, \code{selected_regions},
\code{groups}, and \code{case_group}. For reproducible simulations, call
\code{set.seed()} before \code{simulateDMRs()}.
}
\description{
This simulator is inspired by \code{dmrseq::simDMRs()}, but adds differential
signal on the logit methylation scale. The same regional perturbation engine
is used across methylation assays by operating on methylated-count and
coverage-count representations. For microarray-style beta values, pseudo
counts are constructed first, then the same simulation mechanism is applied.
}
\examples{
beta <- matrix(seq(0.1, 0.9, length.out = 48), nrow = 12)
rownames(beta) <- paste0("cg", seq_len(nrow(beta)))
colnames(beta) <- paste0("sample", seq_len(ncol(beta)))
locs <- data.frame(
    chr = "chr1",
    start = seq(100, by = 100, length.out = nrow(beta)),
    row.names = rownames(beta)
)
set.seed(1)
simulated <- simulateDMRs(
    beta, num_dmrs = 1, min_sites = 5, max_sites = 20,
    sorted_locs = locs, verbose = 0
)
}
