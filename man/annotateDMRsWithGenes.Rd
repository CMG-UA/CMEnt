% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{annotateDMRsWithGenes}
\alias{annotateDMRsWithGenes}
\title{Annotate DMRs with Gene Information}
\usage{
annotateDMRsWithGenes(
  dmrs,
  genome = "hg19",
  promoter_upstream = 2000,
  promoter_downstream = 200
)
}
\arguments{
\item{dmrs}{Dataframe or GRanges object containing DMR coordinates}

\item{genome}{Character. Genome version to use for gene annotation.
Supported values: "hg19", "hg38", "mm10", "mm39" (default: "hg19")}

\item{promoter_upstream}{Integer. Number of base pairs upstream of TSS to
define promoter region (default: 2000)}

\item{promoter_downstream}{Integer. Number of base pairs downstream of TSS
to define promoter region (default: 200)}
}
\value{
The input Dataframe/GRanges object with additional metadata columns:
\itemize{
\item in_promoter_of: Character vector of gene symbols with promoters overlapping the DMR (comma-separated)
\item in_gene_body_of: Character vector of gene symbols with gene bodies overlapping the DMR (comma-separated)
}
}
\description{
Annotates DMRs with overlapping gene promoters and gene bodies
using TxDb annotations. For each DMR, identifies genes whose promoters or
gene bodies overlap with the DMR coordinates.
}
\details{
The function uses genome-appropriate TxDb packages:
\itemize{
\item hg19: TxDb.Hsapiens.UCSC.hg19.knownGene
\item hg38: TxDb.Hsapiens.UCSC.hg38.knownGene
\item mm10: TxDb.Mmusculus.UCSC.mm10.knownGene
\item mm39: TxDb.Mmusculus.UCSC.mm39.knownGene
}

Gene symbols are retrieved from the appropriate org.*.eg.db package.
Multiple overlapping genes are concatenated with commas.
}
\examples{
# Annotate DMRs with gene information
dmrs <- data.frame(
    chr = c("chr1", "chr2"),
    start = c(1000000, 2000000),
    end = c(1001000, 2001000)
)
dmrs_annotated <- annotateDMRsWithGenes(dmrs, genome = "hg19")

# Use custom promoter definition
dmrs_annotated <- annotateDMRsWithGenes(
    dmrs,
    genome = "hg38",
    promoter_upstream = 5000,
    promoter_downstream = 1000
)

}
