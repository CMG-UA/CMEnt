% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_dmps_array.R
\name{findDMPsArray}
\alias{findDMPsArray}
\title{Find DMPs from methylation array beta values using limma}
\usage{
findDMPsArray(
  beta,
  samplesheet,
  samplesheet_sep = "\\t",
  sample_group_col = NULL,
  id_col = "Sample_ID",
  array = NULL,
  genome = NULL,
  sorted_locs = NULL,
  njobs = getOption("CMEnt.njobs", 1L),
  chr = "auto",
  case_group = NULL,
  covariates = NULL,
  output_file = NULL
)
}
\arguments{
\item{beta}{A beta input supported by \code{\link[=getBetaHandler]{getBetaHandler()}}, such as a numeric
matrix/data frame or a beta file path. minfi \code{MethylSet}/\code{RatioSet} inputs
are also accepted and converted to beta values.}

\item{samplesheet}{A data frame or file path to a tab-delimited sample sheet.}

\item{samplesheet_sep}{Separator for samplesheet files. Default is tab.}

\item{sample_group_col}{Column in \code{samplesheet} containing group labels.}

\item{id_col}{Column in \code{samplesheet} containing sample IDs. row.names can also be used by specifying \code{id_col = "row.names"}.}

\item{array}{Array platform passed to \code{\link[=getSortedGenomicLocs]{getSortedGenomicLocs()}} when
\code{sorted_locs} is not supplied.}

\item{genome}{Optional genome passed to \code{\link[=getSortedGenomicLocs]{getSortedGenomicLocs()}} when
\code{sorted_locs} is not supplied. If \code{NULL}, inferred from \code{beta} when
possible, otherwise from \code{array}.}

\item{sorted_locs}{Optional data frame of probe locations with row names as
site IDs and \code{chr} plus \code{start} or \code{pos} columns.}

\item{njobs}{Number of jobs used by \code{\link[=getBetaHandler]{getBetaHandler()}} when reading beta
files.}

\item{chr}{Chromosomes to retain, \code{"auto"} for chr1-chr22, or \code{"all"} for
chr1-chr22 plus chrX and chrY.}

\item{case_group}{Group label to treat as case. If \code{NULL}, the first group in
\code{sample_group_col} is used.}

\item{covariates}{Optional covariate column names, or a comma-separated
string, to include in the limma model.}

\item{output_file}{Optional tab-delimited output path. Files ending in \code{.gz}
are gzipped.}
}
\value{
A data frame with columns \code{chr}, \code{start}, \code{end}, \code{site_id}, \code{pval},
\code{qval}, \code{delta_beta}, and \code{score}.
}
\description{
This helper identifies differentially methylated positions (DMPs) from
methylation array beta values using limma, returning the same seed-style
output columns as \code{\link[=findDMPsBSSeq]{findDMPsBSSeq()}}.
In contrast to \code{\link[minfi:dmpFinder]{dmpFinder()}} from minfi, this function supports flexible covariate inclusion
and returns a consistent output format with the BS-seq DMPs for downstream compatibility with CMEnt's region-finding functions.
}
\examples{
beta <- matrix(
    c(
        0.10, 0.12, 0.11, 0.82, 0.80, 0.81,
        0.74, 0.72, 0.73, 0.20, 0.22, 0.21,
        0.40, 0.42, 0.41, 0.43, 0.44, 0.45
    ),
    nrow = 3,
    byrow = TRUE,
    dimnames = list(paste0("cg", 1:3), paste0("s", 1:6))
)
samplesheet <- data.frame(
    Sample_ID = colnames(beta),
    Sample_Group = rep(c("control", "case"), each = 3)
)
sorted_locs <- data.frame(
    chr = "chr1",
    start = c(100L, 200L, 300L),
    row.names = rownames(beta)
)
dmps <- findDMPsArray(
    beta = beta,
    samplesheet = samplesheet,
    sample_group_col = "Sample_Group",
    array = "450K",
    genome = "hg19",
    sorted_locs = sorted_locs,
    case_group = "case",
    chr = "all"
)
head(dmps)

}
