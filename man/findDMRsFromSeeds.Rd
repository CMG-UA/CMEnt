% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmrs_finder_from_seeds.R
\name{findDMRsFromSeeds}
\alias{findDMRsFromSeeds}
\alias{.buildConnectivityArray}
\title{Find Differentially Methylated Regions (DMRs) from Pre-computed DMPs}
\usage{
.buildConnectivityArray(
  beta_file_handler,
  group_inds,
  sorted_locs,
  max_pval = 0.05,
  min_delta_beta = 0,
  casecontrol = NULL,
  max_lookup_dist = 1000,
  chunk_size = 1000,
  aggfun = median,
  empirical_strategy = "auto",
  pval_mode = "empirical",
  ntries = 500,
  mid_p = TRUE,
  tries_seed = 42,
  njobs = 1,
  verbose = 0
)

findDMRsFromSeeds(
  beta = NULL,
  dmps = NULL,
  pheno = NULL,
  dmps_tsv_id_col = NULL,
  pval_col = "pval_adj",
  sample_group_col = "Sample_Group",
  dmp_group_col = NULL,
  dmp_groups_info = NULL,
  casecontrol_col = NULL,
  min_cpg_delta_beta = 0,
  expansion_step = 500,
  array = c("450K", "27K", "EPIC", "EPICv2"),
  genome = c("hg19", "hg38", "mm10", "mm39"),
  max_pval = 0.05,
  pval_mode = c("parametric", "empirical"),
  empirical_strategy = c("auto", "montecarlo"),
  ntries = 200L,
  mid_p = FALSE,
  tries_seed = NULL,
  max_lookup_dist = 10000,
  min_dmps = 1,
  min_adj_dmps = 1,
  min_cpgs = 50,
  aggfun = c("median", "mean"),
  ignored_sample_groups = NULL,
  output_prefix = NULL,
  njobs = future::availableCores(),
  memory_threshold_mb = 500,
  verbose = 1,
  beta_row_names_file = NULL,
  tabix_file = NULL,
  .load_debug = FALSE
)
}
\arguments{
\item{max_pval}{Numeric. Maximum p-value to assume DMPs correlation is significant. Default is 0.05.}

\item{max_lookup_dist}{Numeric. Maximum distance to look up for adjacent DMPs belonging to the same DMR. Default is 10000.}

\item{aggfun}{Character. Aggregation function to use ("median" or "mean") when calculating delta beta values and p-values of DMRs. Default is "median".}

\item{pval_mode}{Character. "parametric" (default) to use t-based correlation p-values during connectivity testing, or "empirical" to use permutation-based p-values.}

\item{ntries}{Integer. Number of permutations when pval_mode = "empirical". Default is 0 (disabled).}

\item{tries_seed}{Integer or NULL. RNG seed for reproducibility when pval_mode = "empirical". Default is NULL.}

\item{njobs}{Numeric. Number of parallel jobs to use. Default is the number of available cores.}

\item{verbose}{Numeric. Level of verbosity for logging messages, from 0 (not verbose) to 3 (very verbose). Default is 1.}

\item{beta}{Character. Path to the beta value file, or a beta matrix, or a BetaFileHandler object. Either this or tabix_file must be provided.}

\item{dmps}{Character. Path to the DMPs TSV file or the dmps dataframe, in a format like the one produced by dmpFinder.}

\item{pheno}{Data frame. Phenotype data.}

\item{dmps_tsv_id_col}{Character. Column name for DMP identifiers in the DMPs TSV file. Default is NULL.}

\item{pval_col}{Character. Column name for p-values in the DMPs file. Default is "pval_adj".}

\item{sample_group_col}{Character. Column name for sample group information in the phenotype data. Default is NULL.}

\item{dmp_group_col}{Character. Column name for DMP group information in the DMPs TSV file. Default is NULL.}

\item{dmp_groups_info}{Named list. Required when \code{dmp_group_col} is given. List of DMP group information, where names are group identifiers, found in dmps_tsv_id_col column, and values are the samples names, found in the beta values columns. Default is NULL.}

\item{casecontrol_col}{Boolean Column in pheno for case (TRUE/1) / control (FALSE/0) status . If NULL, controls will be assumed to be the first level of sample_group_col. Default is NULL.}

\item{min_cpg_delta_beta}{Numeric. Minimum delta beta value for CpGs. Default is 0.}

\item{expansion_step}{Numeric. Step size for expanding DMRs. Increasing it means higher memory usage and faster computation. Default is 500.}

\item{array}{Character. Type of array used (e.g., "450K", "EPIC", "EPICv2", "27K"). Ignored if using mm10 genome.}

\item{genome}{Character. Genome version (e.g., "hg19", "hg38", "mm10"). Default is "hg19".}

\item{min_dmps}{Numeric. Minimum number of connected DMPs in a DMR. Default is 1.}

\item{min_adj_dmps}{Numeric. Minimum number of DMPs, adjusted by CpG density, in a DMR after extension. Default is 1.}

\item{min_cpgs}{Numeric. Minimum number of CpGs in a DMR after extension. Default is 50.}

\item{ignored_sample_groups}{Character vector. Sample groups to ignore, separated by commas. Default is NULL.}

\item{output_prefix}{Character. Identifier for the output files. If not provided, no output will be saved. Default is NULL.}

\item{memory_threshold_mb}{Numeric. Memory threshold in MB for loading beta files. Default is 500.}

\item{beta_row_names_file}{Character. Path to a file containing row names for the beta values. If not provided, row names will be read from the beta file. Default is NULL.}

\item{tabix_file}{Character. Path to a tabix-indexed beta file. Either this or beta must be provided. Default is NULL.}
}
\value{
A GRanges object containing identified DMRs with metadata columns:
\itemize{
\item cpgs_num: Number of CpGs in the region
\item dmps_num: Number of DMPs in the region
\item delta_beta: Mean methylation difference (aggregated using the specified aggregation function)
\item delta_beta_min: Minimum methylation difference
\item delta_beta_max: Maximum methylation difference
\item delta_beta_sd: Standard deviation of methylation differences
\item pval_adj: Adjusted p-value (if available in DMPs file)
\item cases_beta: Mean beta value in cases
\item controls_beta: Mean beta value in controls
\item start_dmp: ID of the first DMP in the region
\item end_dmp: ID of the last DMP in the region
}

Data frame of identified DMRs.
}
\description{
This function identifies Differentially Methylated Regions (DMRs) from pre-computed
Differentially Methylated Positions (DMPs) using a correlation-based approach. It expands
significant DMPs into regions, considering both statistical significance and biological
relevance of methylation changes.

This function identifies DMRs from a given set of DMPs and a beta value file.
}
\section{Important Note on Input Data}{

Do not apply heavy filtering to your DMPs prior to using this function, particularly based on
beta values or effect sizes. The function works by expanding regions around significant DMPs
and connecting nearby CpGs into larger regions. Filtering out DMPs with smaller effect sizes
may remove important CpGs that could serve as "bridges" to connect more significant DMPs into
larger, biologically meaningful DMRs. For optimal results, include all statistically
significant DMPs (e.g., adjusted p-value < 0.05) and let the function handle region expansion
and filtering internally using the min_cpg_delta_beta parameter if needed.
}

\examples{
\dontrun{
# Load example data
if (!requireNamespace("DMRsegaldata", quietly = TRUE)) {
  remotes::install_github("CMG-UA/DMRsegaldata")
}
library(DMRsegaldata)
beta <- DMRsegaldata::beta
dmps <- DMRsegaldata::dmps
pheno <- DMRsegaldata::pheno
# Find DMRs
dmrs <- findDMRsFromSeeds(
    beta = beta,
    dmps = dmps,
    pheno = pheno
)
}

}
