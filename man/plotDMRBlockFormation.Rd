% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dmrs.R
\name{plotDMRBlockFormation}
\alias{plotDMRBlockFormation}
\title{Plot Intermediate DMR Block Formation Diagnostics}
\usage{
plotDMRBlockFormation(
  dmrs,
  chromosome,
  genome = "hg38",
  k_neighbors = 5L,
  min_segment_size = 2L,
  block_gap_mode = "adaptive",
  block_gap_fixed_bp = NULL,
  block_gap_quantile = 0.95,
  block_gap_multiplier = 1.5,
  block_gap_min_bp = 250000,
  block_gap_max_bp = 5e+06,
  point_alpha = 0.7,
  point_size = 1
)
}
\arguments{
\item{dmrs}{GRanges object or data frame. DMR results from \code{findDMRsFromSeeds}
or \code{scoreDMRs}.}

\item{chromosome}{Character. Chromosome to inspect (e.g., \code{"chr7"} or \code{"7"}).}

\item{genome}{Character. Genome version passed to \code{convertToGRanges}
(default: \code{"hg38"}).}

\item{k_neighbors}{Integer. Number of nearest neighbors used in adaptive Gaussian
smoothing (default: \code{5}).}

\item{min_segment_size}{Integer. Minimum size of linear segments in PELT
segmentation (default: \code{2}).}

\item{block_gap_mode}{Character. Gap rule for block splitting:
\code{"adaptive"} (default), \code{"fixed"}, or \code{"none"}.}

\item{block_gap_fixed_bp}{Numeric. Maximum allowed midpoint gap (bp) when
\code{block_gap_mode = "fixed"}. Ignored otherwise.}

\item{block_gap_quantile}{Numeric in \verb{(0, 1)}. Gap quantile used for adaptive
thresholding (default: \code{0.95}).}

\item{block_gap_multiplier}{Numeric > 0. Multiplier for adaptive gap threshold
(default: \code{1.5}).}

\item{block_gap_min_bp}{Numeric >= 0. Lower clamp for adaptive gap threshold (bp).
Default is \code{250000}.}

\item{block_gap_max_bp}{Numeric >= \code{block_gap_min_bp}. Upper clamp for adaptive
gap threshold (bp). Default is \code{5000000}.}

\item{point_alpha}{Numeric. Alpha for raw score points in \verb{[0, 1]}
(default: \code{0.7}).}

\item{point_size}{Numeric. Point size for raw scores (default: \code{1.0}).}
}
\value{
A ggplot object.
}
\description{
Visualizes how DMR blocks are formed on a single chromosome by
layering raw scores, smoothed scores, piecewise-linear segments, slope-based
candidate blocks, distance-based split boundaries, and final accepted blocks.
}
\examples{
\donttest{
dmrs <- readRDS("dmrs.rds")
p <- plotDMRBlockFormation(dmrs, chromosome = "chr7")
print(p)
}
}
