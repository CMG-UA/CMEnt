% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_pvalues.R
\name{combinePvalues}
\alias{combinePvalues}
\title{Combine spatially correlated p-values with comb-p style SLK correction}
\usage{
combinePvalues(
  pvals,
  positions,
  chr = NULL,
  ends = positions,
  max_dist = 1000,
  lag_step = 50,
  min_dist = 0,
  lags = NULL,
  acf = NULL,
  method = c("slk", "none", stats::p.adjust.methods),
  correlation = c("spearman", "pearson", "kendall"),
  transform = TRUE,
  partial = TRUE,
  p_floor = 9e-117,
  na_correlation = 0
)
}
\arguments{
\item{pvals}{Numeric vector of p-values.}

\item{positions}{Numeric vector of genomic positions, one per p-value.}

\item{chr}{Optional chromosome vector. If \code{NULL}, all positions are treated
as coming from the same chromosome.}

\item{ends}{Optional end positions. Defaults to \code{positions}, which treats
each locus as a single point.}

\item{max_dist}{Maximum distance used for local SLK neighborhoods and ACF
estimation when \code{acf} is not supplied. Default is 1000.}

\item{lag_step}{Width of distance bins used to estimate the ACF when \code{acf}
is not supplied. Default is 50.}

\item{min_dist}{Lower bound of the first ACF distance bin. Default is 0.}

\item{lags}{Optional numeric vector of lag breakpoints. If supplied, it
overrides \code{min_dist}, \code{max_dist}, and \code{lag_step} for ACF estimation.}

\item{acf}{Optional precomputed ACF table with lag minimum, lag maximum, and
correlation columns.}

\item{method}{Either \code{"slk"}/\code{"none"} to return SLK p-values, or any method
accepted by \code{\link[stats:p.adjust]{stats::p.adjust()}} such as \code{"fdr"}/\code{"BH"} to adjust the SLK
p-values.}

\item{correlation}{Correlation method used for ACF estimation. Default is
\code{"spearman"}, matching comb-p.}

\item{transform}{Logical. If \code{TRUE}, estimate correlations on \code{-log10(p)}.
Default is \code{TRUE}, matching comb-p.}

\item{partial}{Logical. If \code{TRUE}, estimate each lag interval from only
pairs in that interval. If \code{FALSE}, outer intervals include pairs from
shorter distances as in comb-p's non-partial ACF mode.}

\item{p_floor}{Minimum p-value used before q-normal transformation.}

\item{na_correlation}{Correlation value used for ACF bins whose correlation
cannot be estimated. Default is 0.}
}
\value{
A numeric vector in the same order as \code{pvals}. The estimated or
supplied ACF is attached as the \code{"acf"} attribute, and the unadjusted SLK
p-values are attached as \code{"slk_pvalues"} when \code{method} requests an
additional multiple-testing adjustment.
}
\description{
\code{combinePvalues()} is an R port of the comb-p Stouffer-Liptak-Kechris
(SLK) p-value correction step. For each locus, nearby loci within
\code{max_dist} are collected, an autocorrelation-derived covariance term is
computed from their genomic distances, and the local p-values are combined
with the SLK z-score formula used by comb-p.
}
\details{
If \code{acf} is not supplied, lagged correlations are estimated from \code{pvals} and
\code{positions} using comb-p's default convention of Spearman correlations on
\code{-log10(p)}. If \code{acf} is supplied, it should contain columns named
\code{lag_min}, \code{lag_max}, and \code{correlation}, or have those values in its first
three columns.
}
\examples{
p <- c(0.01, 0.03, 0.4, 0.02)
pos <- c(100, 150, 900, 940)
combinePvalues(p, pos, max_dist = 100)

}
\references{
Pedersen BS, Schwartz DA, Yang IV, Kechris KJ. Comb-p: software for
combining, analyzing, grouping and correcting spatially correlated P-values.
Bioinformatics. 2012;28(22):2986-2988.
}
