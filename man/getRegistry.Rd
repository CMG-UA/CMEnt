% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{getRegistry}
\alias{getRegistry}
\alias{[.Registry}
\alias{as.data.frame.Registry}
\alias{dim.Registry}
\alias{names.Registry}
\alias{dimnames.Registry}
\alias{colnames.Registry}
\alias{row.names.Registry}
\alias{[[.Registry}
\alias{$.Registry}
\title{Create a registry for indexed tabular retrieval}
\usage{
getRegistry(
  data,
  indices = list(),
  select = NULL,
  rename = NULL,
  derive = NULL,
  master_index = NULL,
  memory_threshold_mb = getOption("DMRsegal.registry_in_mem_threshold_mb", 500),
  chunk_size = getOption("DMRsegal.registry_chunk_size", 50000),
  sqlite_path = NULL,
  query_chunk_size = getOption("DMRsegal.registry_query_chunk_size", 50000)
)

\method{[}{Registry}(x, i, j, drop = FALSE)

\method{as.data.frame}{Registry}(x, ...)

\method{dim}{Registry}(x)

\method{names}{Registry}(x)

\method{dimnames}{Registry}(x)

\method{colnames}{Registry}(x, do.NULL = TRUE, prefix = "col")

\method{row.names}{Registry}(x, do.NULL = TRUE, prefix = "row")

\method{[[}{Registry}(x, i, ...)

\method{$}{Registry}(x, name)
}
\arguments{
\item{data}{Path to a TSV file (plain or gzipped, e.g. \code{.tsv.gz}) or a \code{data.frame}.}

\item{indices}{Named list of predefined index definitions. Each list element
must be a character vector with one or more column names.}

\item{select}{Optional column selector applied at ingestion time. Can be
\code{NULL}, a character vector of column names, or a positive integer vector of
column positions.}

\item{rename}{Optional rename map applied at ingestion time. Accepts a named
character vector or named list. Preferred form is \code{old = "new"} (old
column names as names, new names as values). A \code{new = "old"} style is also
accepted when unambiguous.}

\item{derive}{Optional named list defining derived columns to add at ingestion
time. Each element name is the new column name. Each element value can be:
\itemize{
\item a function taking the current data.frame chunk and returning a vector of
length \code{nrow(chunk)}, or
\item a list with \code{cols} (character vector of input columns) and \code{fun}
(function), called as \code{do.call(fun, chunk[cols])}.
}}

\item{master_index}{Optional name of the index used for character-based row
subsetting (first position in \code{[}).
If \code{NULL} and exactly one single-column index is available, that index is
used automatically.}

\item{memory_threshold_mb}{Numeric. Inputs with estimated size greater than
this threshold are stored in SQLite.}

\item{chunk_size}{Integer. Number of rows per chunk for SQLite ingestion.}

\item{sqlite_path}{Optional SQLite file path. If \code{NULL}, a temporary file is used.}

\item{query_chunk_size}{Integer. Number of rows per chunk for temporary query
tables (lookup keys and active rows) in SQLite mode.}

\item{x}{A \code{Registry} object.}

\item{i}{Row selector relative to the current active row bounds. Character
selectors use \code{master_index}.}

\item{j}{Column selector relative to the current active column bounds.}

\item{drop}{Included for compatibility. Ignored.}

\item{...}{Passed to \code{Registry$as.data.frame()}.}
}
\value{
A \code{Registry} object.
}
\description{
Builds an index-aware registry from a TSV file or a \code{data.frame}.
Depending on the estimated input size, data is kept in memory or persisted to
SQLite. The registry keeps active row/column bounds. Subsetting returns a
new bounded \code{Registry} view applied to all subsequent queries.

The row names of a \code{Registry} correspond to the master index
values for the currently active row bounds. If no master index is defined,
row names are generated as \code{row1}, \code{row2}, etc.
}
\examples{
df <- data.frame(
    id = c("a", "b", "c"),
    group = c("g1", "g1", "g2"),
    value = c(1, 2, 3),
    stringsAsFactors = FALSE
)

reg <- getRegistry(
    data = df,
    indices = list(id = "id", by_group = "group")
)

reg$queryOnIndex("id", c("c", "a"))
reg_view <- reg[1:2, c("id", "value")]
as.data.frame(reg_view)
}
