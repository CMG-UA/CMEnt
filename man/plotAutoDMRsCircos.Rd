% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dmrs_circos.R
\name{plotAutoDMRsCircos}
\alias{plotAutoDMRsCircos}
\title{Plot DMR Circos Views Using Automatically Selected Regions}
\usage{
plotAutoDMRsCircos(
  dmrs,
  beta,
  pheno,
  method = c("blocks", "components", "hybrid", "quick"),
  n_regions = 6,
  region_flank_bp = 1e+06,
  max_regions_per_chr = 2,
  min_inter_region_bp = 5e+06,
  genome = "hg38",
  array = "450K",
  sorted_locs = NULL,
  components = NULL,
  interactions = NULL,
  min_similarity = 0.8,
  motif_cpg_flank_size = 5,
  min_component_size = 2,
  chromosomes = NULL,
  query_components_with_jaspar = TRUE,
  ...
)
}
\arguments{
\item{dmrs}{GRanges object or data frame. DMR results from findDMRsFromSeeds.}

\item{beta}{BetaHandler object, character path to beta file, or beta values matrix.}

\item{pheno}{Data frame or character path to phenotype file. Sample information with
rownames matching beta column names (required for beta track).}

\item{method}{Character. Automatic region selection mode: \code{"blocks"} (default),
\code{"components"}, \code{"hybrid"}, or \code{"quick"}.}

\item{n_regions}{Integer. Target maximum number of regions to show (default: \code{6}).}

\item{region_flank_bp}{Numeric. Flank in base pairs added around each selected
block or DMR before plotting (default: \code{1e6}).}

\item{max_regions_per_chr}{Integer or \code{NULL}. Per-chromosome cap on automatically
selected regions (default: \code{2}).}

\item{min_inter_region_bp}{Numeric. Nearby candidate windows within this gap are
merged instead of being shown as separate Circos sectors (default: \code{5e6}).}

\item{genome}{Character. Genome version (e.g., "hg38").}

\item{array}{Character. Array platform type (default: "450K"). Ignored if sorted_locs is provided.}

\item{sorted_locs}{Data frame. Genomic locations sorted by position (optional). If NULL, will be fetched based on array and genome.}

\item{components}{Data frame. Output from motif component detection (optional, will be computed if missing).}

\item{interactions}{Data frame. Output from motif interaction detection (optional, will be computed if missing).}

\item{min_similarity}{Numeric. Minimum motifs PWM similarity threshold for considering DMRs are related (default: 0.8).}

\item{motif_cpg_flank_size}{Integer. Flanking region size for motif extraction in bp (default: 5).}

\item{min_component_size}{Integer. Minimum motif component size to retain (default: 2).}

\item{chromosomes}{Character vector. Subset of chromosomes to display (default: NULL, show all available).}

\item{query_components_with_jaspar}{Logical. Whether computed motif components should be queried against JASPAR
before plotting (default: \code{TRUE}). Set to \code{FALSE} to keep link computation cheaper.}

\item{...}{Additional arguments passed to \code{\link[=plotDMRsCircos]{plotDMRsCircos()}}. This is where
plot-only settings such as \code{max_dmrs_per_chr}, \code{max_cpgs_per_dmr},
\code{max_num_samples_per_group}, \code{max_components}, \code{unmatched_interaction_color},
\code{legend_width_ratio}, \code{degenerate_resolution}, \code{output_file}, and \code{verbose}
can be supplied. Arguments managed by automatic selection, including \code{region},
should not be passed through \code{...}.}
}
\value{
Invisibly returns the selected region data frame with columns \code{chr},
\code{start}, and \code{end}.
}
\description{
Selects a small set of informative genomic windows from the input DMRs
and forwards them to \code{\link[=plotDMRsCircos]{plotDMRsCircos()}}. The default \code{blocks} mode prefers
localized scoring DMR blocks when \code{block_id} is available, while \code{quick} mode
falls back to top-scoring individual DMR windows. Both selectors are greedy and
avoid exhaustive optimization to keep region finding inexpensive.
}
\examples{
\dontrun{
plotAutoDMRsCircos(dmrs, beta = "beta.txt", pheno = pheno_df)
plotAutoDMRsCircos(dmrs, beta = "beta.txt", pheno = pheno_df, method = "blocks")
plotAutoDMRsCircos(dmrs, beta = "beta.txt", pheno = pheno_df, method = "components")
plotAutoDMRsCircos(dmrs, beta = "beta.txt", pheno = pheno_df, method = "hybrid")
plotAutoDMRsCircos(dmrs, beta = "beta.txt", pheno = pheno_df, method = "quick", n_regions = 4)
}
}
