% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{preprocessEPICv2}
\alias{preprocessEPICv2}
\title{Preprocess EPICv2 Beta Values Using DMRcate}
\usage{
preprocessEPICv2(
  beta,
  epicv2Remap = TRUE,
  epicv2Filter = c("mean", "sensitivity", "precision", "random")
)
}
\arguments{
\item{beta}{Matrix of beta values with EPICv2 probe IDs as rownames.
Probe IDs should have 15 characters (e.g., cg00000029_TC21).}

\item{epicv2Remap}{Logical. Remap cross-hybridizing probes to their likely targets
based on whole-genome bisulfite sequencing (WGBS) evidence. Default TRUE.
See DMRcate::cpg.annotate() for details.}

\item{epicv2Filter}{Character. Strategy for handling replicate probes that map to
the same CpG site after remapping:
\itemize{
\item "mean": Average beta values across replicates (default)
\item "sensitivity": Select probe most sensitive to methylation changes
\item "precision": Select probe with best measurement precision
\item "random": Randomly select one probe per position
}}
}
\value{
List with:
\item{beta}{Preprocessed beta matrix with filtered/aggregated probes}
\item{coords}{Data frame with chr, start, end for each probe (remapped if applicable)}
\item{probe_mapping}{Data frame mapping original probe IDs to retention status}
}
\description{
Preprocesses EPICv2 array data using DMRcate's cpg.annotate() function.
This handles cross-hybridizing probe remapping and replicate probe filtering.

EPICv2 arrays contain probes that cross-hybridize to locations different from their
intended targets. Additionally, after remapping, multiple probes may target the same
CpG site. This function uses DMRcate's methodology to address both issues.
}
\details{
This function uses DMRcate (Peters et al.) for EPICv2-specific preprocessing.
DMRcate will be installed automatically if not present.

Credit: DMRcate package by Tim J. Peters et al.
}
\examples{
\dontrun{
# Preprocess EPICv2 beta values
result <- preprocessEPICv2(epicv2_beta, epicv2Remap = TRUE, epicv2Filter = "mean")

# Access preprocessed data
preprocessed_beta <- result$beta
remapped_coords <- result$coords
}

}
\references{
Peters, T.J., Meyer, B., Ryan, L., et al. (2024). Characterisation and reproducibility
of the HumanMethylationEPIC v2.0 BeadChip for DNA methylation profiling.
BMC Genomics, 25, 251. doi:10.1186/s12864-024-10027-5
}
