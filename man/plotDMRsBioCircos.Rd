% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dmrs.R
\name{plotDMRsCircos}
\alias{plotDMRsCircos}
\title{Plot BioCircos Visualization of DMRs}
\usage{
plotDMRsCircos(
  dmrs,
  beta,
  pheno,
  array = c("450K", "27K", "EPIC", "EPICv2"),
  genome = c("hg19", "hg38", "mm10", "mm39"),
  sample_group_col = "Sample_Group",
  plot_interactions = TRUE,
  min_sim = 0.7,
  flank_size = 5,
  max_cpgs_per_dmr = 100,
  genome_label = NULL,
  ...
)
}
\arguments{
\item{dmrs}{GRanges object or data frame. DMR results from findDMRsFromSeeds.}

\item{beta}{BetaHandler object, character path to beta file, or beta values matrix.}

\item{pheno}{Data frame or character path to phenotype file. Sample information with
rownames matching beta column names (required for beta track).}

\item{array}{Character. Array platform type (default: "450K").}

\item{genome}{Character. Genome version (e.g., "hg19", "hg38", "mm10", "mm39"). Supports any genome with a corresponding TxDb package.}

\item{sample_group_col}{Character. Column in pheno for sample grouping (default: "Sample_Group").}

\item{plot_interactions}{Logical. Whether to compute and plot motif-based DMR interactions (default: TRUE).}

\item{max_fdr}{Numeric. Minimum FDR threshold for motif-based interactions (default: 0.05).}

\item{flank_size}{Integer. Flanking region size for motif extraction in bp (default: 5).}

\item{max_cpgs_per_dmr}{Integer. Maximum number of CpGs to show per DMR in scatter/heatmap (default: 100).}

\item{genome_label}{Character. Label for the genome (default: genome parameter value).}

\item{chr_order}{Character vector. Order of chromosomes to display (default: chr1-22, X, Y).}

\item{...}{Additional arguments passed to BioCircos functions.}
}
\value{
A BioCircos plot object.
}
\description{
Creates a circular genome plot (BioCircos) showing DMRs with multiple tracks:
from outer to inner: scatter plots of DMPs (red) and supporting CpGs (gray),
beta value heatmaps, DMR arcs, and motif-based interaction links between DMRs.
}
\examples{
# Load DMR results
dmrs <- readRDS("dmrs.rds")

# Basic BioCircos plot
plotDMRsCircos(dmrs, beta = "beta.txt", pheno = pheno_df)

# Without interactions
plotDMRsCircos(dmrs, beta = "beta.txt", pheno = pheno_df, plot_interactions = FALSE)

}
