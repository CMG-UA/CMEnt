% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_dmrs_bsseq.R
\name{simulateDMRsBSSeq}
\alias{simulateDMRsBSSeq}
\title{Simulate DMRs in a BSseq object while preserving local structure}
\usage{
simulateDMRsBSSeq(
  bs,
  num_dmrs = 3000L,
  delta_max0 = 0.3,
  groups = NULL,
  case_group = NULL,
  max_gap = 500L,
  min_cpgs = 5L,
  max_cpgs = 500L,
  truth_min_delta_beta = 0.05,
  delta_jitter = 1/3,
  profile = c("triweight", "flat"),
  profile_degree = 4L,
  flank_fraction = 0.2,
  neutral_region_sd = 0,
  matched_null_regions = 0L,
  seed = NULL,
  rename_samples = TRUE,
  resample_counts = TRUE
)
}
\arguments{
\item{bs}{A BSseq object.}

\item{num_dmrs}{Number of DMRs to spike in.}

\item{delta_max0}{Baseline maximum beta-scale effect size near the center of
each DMR.}

\item{groups}{Optional sample group vector. If \code{NULL}, the first half of
samples are assigned to \code{Condition1} and the remaining samples to
\code{Condition2}.}

\item{case_group}{Group receiving the differential shift. Defaults to the
second group level.}

\item{max_gap}{Maximum gap, in bp, used to form candidate CpG clusters.}

\item{min_cpgs}{Minimum number of CpGs per candidate DMR cluster.}

\item{max_cpgs}{Maximum number of CpGs per candidate DMR cluster.}

\item{truth_min_delta_beta}{Minimum intended beta-scale perturbation for a
CpG to define the reported truth interval. Set to \code{0} to report the full
selected cluster.}

\item{delta_jitter}{Width of the random effect-size jitter around
\code{delta_max0}.}

\item{profile}{Shape of the regional effect, either \code{"triweight"} or
\code{"flat"}.}

\item{profile_degree}{Degree used by the triweight profile.}

\item{flank_fraction}{Fraction of the selected cluster width added on both
sides before evaluating the triweight profile. This mimics
\code{dmrseq::simDMRs()} and avoids a hard zero at the cluster edges.}

\item{neutral_region_sd}{Optional logit-scale regional sample effect applied
to both groups before the differential shift. The default is \code{0}, which
avoids adding DMR-specific correlation beyond what is already present in
\code{bs}.}

\item{matched_null_regions}{Number of non-DMR candidate clusters per DMR that
should receive only the neutral regional sample effect when
\code{neutral_region_sd > 0}. This can be used to avoid making local correlation
unique to true DMRs.}

\item{seed}{Optional random seed.}

\item{rename_samples}{If \code{TRUE}, samples are reordered and renamed using the
\code{dmrseq::simDMRs()} convention: controls first as \code{Condition1_Rep1},
\code{Condition1_Rep2}, etc., followed by cases as \code{Condition2_Rep1},
\code{Condition2_Rep2}, etc.}

\item{resample_counts}{If \code{TRUE}, methylated counts in touched regions are
redrawn from the shifted probabilities and original coverage. If \code{FALSE},
counts are rounded deterministically.}

\item{num.dmrs, deltamax0, delta.max0}{Compatibility aliases for common
\code{dmrseq::simDMRs()} argument names.}
}
\value{
A list with \code{bs}, \code{gr.dmrs}, \code{dmr.mncov}, \code{dmr.L}, \code{delta},
\code{truth}, \code{selected_regions}, \code{groups}, and \code{case_group}.
}
\description{
This is a BSseq DMR simulator in the spirit of \code{dmrseq::simDMRs()}, but the
differential signal is added on the logit methylation scale instead of by
independently perturbing CpG-level beta values. Existing sample-to-sample and
local CpG structure in the input object is therefore preserved inside the
spiked regions, while the truth DMRs remain ordinary regional mean shifts.
}
