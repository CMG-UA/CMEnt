% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dmrs_circos.R
\name{plotDMRsCircos}
\alias{plotDMRsCircos}
\title{Plot Circos Visualization of DMRs}
\usage{
plotDMRsCircos(
  dmrs,
  beta,
  pheno,
  genome = "hg38",
  array = "450K",
  sorted_locs = NULL,
  components = NULL,
  interactions = NULL,
  sample_group_col = "Sample_Group",
  min_similarity = 0.8,
  motif_site_flank_size = 5,
  max_num_samples_per_group = 5,
  max_dmrs_per_chr = 10,
  max_sites_per_dmr = 5,
  min_component_size = 2,
  max_components = 30,
  chromosomes = NULL,
  region = NULL,
  query_components_with_jaspar = TRUE,
  unmatched_interaction_color = "#B3B3B3",
  legend_width_ratio = 0.34,
  degenerate_resolution = 1e+06,
  output_file = NULL,
  verbose = NULL,
  ...
)
}
\arguments{
\item{dmrs}{GRanges object or data frame. DMR results from findDMRsFromSeeds.}

\item{beta}{BetaHandler object, character path to beta file, or beta values matrix.}

\item{pheno}{Data frame or character path to phenotype file. Sample information with
rownames matching beta column names (required for beta track).}

\item{genome}{Character. Genome version (e.g., "hg38").}

\item{array}{Character. Array platform type (default: "450K"). Ignored if sorted_locs is provided.}

\item{sorted_locs}{Data frame. Genomic locations sorted by position (optional). If NULL, will be fetched based on array and genome.}

\item{components}{Data frame. Output from motif component detection (optional, will be computed if missing).}

\item{interactions}{Data frame. Output from motif interaction detection (optional, will be computed if missing).}

\item{sample_group_col}{Character. Column in pheno for sample grouping (default: "Sample_Group").}

\item{min_similarity}{Numeric. Minimum motifs PWM similarity threshold for considering DMRs are related (default: 0.8).}

\item{motif_site_flank_size}{Integer. Flanking region size for motif extraction in bp (default: 5).}

\item{max_num_samples_per_group}{Integer. Maximum number of samples to show per group in heatmap (default: 5).}

\item{max_dmrs_per_chr}{Integer. Maximum number of DMRs to use per chromosome (default: 10). The DMRs with highest absolute delta beta will be selected.}

\item{max_sites_per_dmr}{Integer. Maximum number of CpGs to show per DMR in scatter/heatmap (default: 5).}

\item{min_component_size}{Integer. Minimum motif component size to retain (default: 2).}

\item{max_components}{Integer. Maximum number of interactions to plot (default: 30).}

\item{chromosomes}{Character vector. Subset of chromosomes to display (default: NULL, show all available).}

\item{region}{Genomic region to display. Can be NULL, a GRanges, a string in the form \code{chr:start-end},
or a data.frame/list with columns \code{chr}, \code{start}, \code{end}.}

\item{query_components_with_jaspar}{Logical. Whether computed motif components should be queried against JASPAR
before plotting (default: \code{TRUE}). Set to \code{FALSE} to keep link computation cheaper.}

\item{unmatched_interaction_color}{Character. Color used for interaction components without JASPAR matches.
These links are shown but omitted from the interaction legend (default: \code{"#B3B3B3"}).}

\item{legend_width_ratio}{Numeric. Fraction of horizontal canvas reserved for legends (default: 0.34).}

\item{degenerate_resolution}{Integer. Resolution in base pairs for simplifying narrow glyphs:
link ribbons are drawn as lines when both anchors are below this span, and DMR arcs
are drawn as lines instead of rectangles below this span (default: 1e6).}

\item{output_file}{Character or NULL. Optional PDF path for the plot.}

\item{verbose}{Numeric. Optional verbosity override.}

\item{...}{Additional arguments (currently unused).}
}
\value{
\code{NULL} invisibly after drawing the plot.
}
\description{
Creates a circular genome plot using circlize showing DMRs with multiple tracks:
ideogram track with chromosome bands, DMR arcs colored by delta beta,
beta value heatmaps for each sample, and motif-based interaction links between DMRs.
}
\examples{
\dontrun{
dmrs <- readRDS("dmrs.rds")
plotDMRsCircos(dmrs, beta = "beta.txt", pheno = pheno_df)
plotDMRsCircos(dmrs, beta = "beta.txt", pheno = pheno_df, genome = "hg38")
}

}
