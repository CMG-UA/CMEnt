% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmrs_finder_from_dmps.r
\name{findDMRsFromDMPs}
\alias{findDMRsFromDMPs}
\alias{.get.beta.col.names.and.inds}
\title{Find Differentially Methylated Regions (DMRs) from Pre-computed DMPs}
\usage{
.get.beta.col.names.and.inds(beta.file, beta.col.names = NULL, is.tabix = F)

findDMRsFromDMPs(
  beta.file,
  dmps.tsv.file,
  pheno,
  output.dir = ".",
  pval.col = "pval_adj",
  sample_group.col = "Sample_Group",
  casecontrol.col = "casecontrol",
  min.cpg.delta_beta = 0,
  expansion.step = 50,
  array = c("450K", "EPIC"),
  genome = c("hg19", "hg38"),
  max.pval = 0.05,
  max.lookup.dist = 10000,
  min.dmps = 1,
  min.cpgs = 50,
  ignored.sample.groups = NULL,
  output.id = NULL,
  njobs = parallel::detectCores(),
  verbose = FALSE,
  beta.row.names.file = NULL,
  dmps.beta.file = NULL,
  tabix.file = NULL
)
}
\arguments{
\item{beta.file}{Character. Path to the beta value file.}

\item{dmps.tsv.file}{Character. Path to the DMPs TSV file.}

\item{pheno}{Data frame. Phenotype data.}

\item{output.dir}{Character. Directory to save the output files. Default is current directory.}

\item{pval.col}{Character. Column name for p-values in the DMPs file. Default is "pval_adj".}

\item{sample_group.col}{Character. Column name for sample group information in the phenotype data. Default is NULL.}

\item{casecontrol.col}{Character. Column name for case-control information in the phenotype data. Default is "casecontrol".}

\item{min.cpg.delta_beta}{Numeric. Minimum delta beta value for CpGs. Default is 0.}

\item{expansion.step}{Numeric. Step size for expanding DMRs. Increasing it means higher memory usage and faster computation. Default is 500.}

\item{array}{Character. Type of array used ("450K" or "EPIC"). Default is "450K".}

\item{genome}{Reference genome, "hg19" or "hg38" (default: c("hg19", "hg38"))}

\item{max.pval}{Numeric. Maximum p-value to assume DMPs correlation is significant. Default is 0.05.}

\item{max.lookup.dist}{Numeric. Maximum distance to look up for adjacent DMPs belonging to the same DMR. Default is 10000.}

\item{min.dmps}{Numeric. Minimum number of DMPs in a DMR. Default is 1.}

\item{min.cpgs}{Numeric. Minimum number of CpGs in a DMR. Default is 50.}

\item{ignored.sample.groups}{Sample groups to ignore (default: NULL)}

\item{output.id}{Character. Identifier for the output files. Default is NULL.}

\item{njobs}{Numeric. Number of parallel jobs to use. Default is the number of available cores.}

\item{verbose}{Logical. Whether to print detailed messages. Default is FALSE.}

\item{beta.row.names.file}{Optional file with beta value row names (default: NULL)}

\item{dmps.beta.file}{Optional separate beta file for DMPs (default: NULL)}
}
\value{
A GRanges object containing identified DMRs with metadata columns:
\itemize{
\item n_cpgs: Number of CpGs in the region
\item n_dmps: Number of DMPs in the region
\item mean_delta_beta: Mean methylation difference
\item max_delta_beta: Maximum methylation difference
\item min_pval: Minimum p-value of DMPs in region
}

Data frame of identified DMRs.
}
\description{
This function identifies Differentially Methylated Regions (DMRs) from pre-computed
Differentially Methylated Positions (DMPs) using a correlation-based approach. It expands
significant DMPs into regions, considering both statistical significance and biological
relevance of methylation changes.

This function identifies DMRs from a given set of DMPs and a beta value file.
}
\examples{
\dontrun{
# Load example data
data(example_beta)
data(example_dmps)
data(example_pheno)

# Write beta values to file
beta_file <- tempfile(fileext = ".txt")
write.table(cbind(ID = rownames(example_beta), example_beta),
            file = beta_file, sep = "\t", quote = FALSE, row.names = FALSE)

# Find DMRs
dmrs <- findDMRsFromDMPs(
  beta.file = beta_file,
  dmps.tsv.file = example_dmps,
  pheno = example_pheno
)
}

}
